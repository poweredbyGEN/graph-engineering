.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
dist/
build/

# Local graph execution state. Workflow definitions belong in source; claims and runs do not.
task-graph/
.task-graph/

# Site config — machine-specific paths and repo lists. Never committed.
config.toml

# Private graph worker definitions. Checked-in .graph-engineering.toml is routing-only.
.graph-engineering.local.toml
