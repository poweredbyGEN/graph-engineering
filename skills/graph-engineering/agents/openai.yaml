interface:
  display_name: "Graph Engineering"
  short_description: "Build faster with tested agent graphs"
  default_prompt: "Use $graph-engineering to turn this development task into a bounded, verified agent graph and execute it."
